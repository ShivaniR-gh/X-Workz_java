class MakeUpKitRunner {
    public static void main(String[] args) {
        MakeUpKit.getBrand();
        MakeUpKit.getNumberOfItems();
        MakeUpKit.isWaterproof();
        MakeUpKit.getKitType();
        MakeUpKit.getPrice();
        MakeUpKit.includesMirror();
        MakeUpKit.getInfo();
    }
}
